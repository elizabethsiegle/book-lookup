{
  "name": "book-lookup",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Chrome extension: one textbox, three book sites.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "jsdom": "^30.0.1",
    "vitest": "^4.1.10"
  }
}
